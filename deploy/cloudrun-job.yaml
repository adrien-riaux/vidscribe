apiVersion: run.googleapis.com/v1
kind: Job
metadata:
  name: job-vidscribe
  labels:
    cloud.googleapis.com/location: europe-west1
spec:
  template:
    spec:
      template:
        spec:
          serviceAccountName: vidscribe-job-runner@${PROJECT_ID}.iam.gserviceaccount.com
          timeoutSeconds: 600
          containers:
          - image: ${IMAGE_URL}
            resources:
              limits:
                cpu: '1'
                memory: '512Mi'
            env:
            - name: YOUTUBE_CHANNEL_ID
              value: ${YOUTUBE_CHANNEL_ID}
            - name: STATE_BUCKET
              value: "bucket-vidscribe"
            - name: EMAIL_FROM
              value: ${EMAIL_FROM}
            - name: EMAIL_TO
              value: ${EMAIL_TO}
            - name: SMTP_USER
              value: ${EMAIL_FROM}
            - name: YOUTUBE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: youtube-api-key-vidscribe
                  key: "latest"
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: gemini-api-key-vidscribe
                  key: "latest"
            - name: SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smtp-password-vidscribe
                  key: "latest"
